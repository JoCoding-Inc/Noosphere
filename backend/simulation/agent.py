# backend/simulation/agent.py
#
# NOTE: This module previously contained a react() function for single-idea
# evaluation. That function is no longer used — all agent behaviour now goes
# through social_rounds.py (decide_action → generate_content pipeline).
# The file is kept as a placeholder in case per-agent evaluation is
# reintroduced in a future iteration.

